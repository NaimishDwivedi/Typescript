var user = {
    name: "naimish",
    email: "nd1372003@gmail.com",
    isActive: true
};
//objects are mainly used with the functions
function createUser(_a) {
    var string = _a.name, boolean = _a.isPaid;
}
createUser({ name: "Naimish", isPaid: 3 });
